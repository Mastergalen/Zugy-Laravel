cd ../../
composer dump-autoload
composer install
php artisan migrate 
php artisan db:seed
echo "Don't forget to update .env!"

# Only to be run on dev machine outside Homestead
# npm install --global gulp
# npm install --no-bin-links
